{
  "name": "fluentai",
  "version": "1.0.0",
  "description": "FluentAI - English Learning Platform MERN Stack",
  "author": "Development Team",
  "license": "MIT",
  "engines": {
    "node": ">=22.12.0 <25"
  },
  "workspaces": [
    "server",
    "client"
  ],
  "scripts": {
    "install-all": "npm install",
    "dev": "concurrently \"npm --workspace server run dev\" \"npm --workspace client run start\"",
    "server": "npm --workspace server run dev",
    "client": "npm --workspace client run start",
    "seed": "npm --workspace server run seed",
    "build": "npm --workspace server run build && npm --workspace client run build",
    "test": "npm --workspace server run test"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "playwright-core": "^1.60.0"
  }
}
